# 2026-02-28 工作计划

## 当前进度总结

**任务完成率**: 20/42 (48%)

- ✅ 完成: 19个任务 (P1.1-P1.4全部完成)
- ⏳ 待进行: 21个任务 
- 🚫 阻塞: 2个任务 (现已更新)
- 🔧 已修复所有关键缺陷（Parser分号、字符串去引号、SSA优化）

## 已验证的功能

- ✅ **Parser分号/换行处理** - 多命令序列正确解析
  
  - 测试: `echo "test1"; echo "test2"` ✓
  - 输出: 正确分别输出test1、test2

- ✅ **字符串去引号** - 双引号正确移除
  
  - 测试: `echo "hello world"` ✓
  - 输出: `hello world` (无多余引号)

- ✅ **函数定义框架** - 单行函数定义可解析
  
  - 测试: `test_func() { echo "hello"; }` ✓
  - 注意: 多行函数定义需要交互式多行模式支持

- ✅ **SSA IR清理** - 无意义const赋值已移除
  
  - 验证: IR输出简洁，没有多余%1/%2/%3

## 下一优先级任务

### 🎯 P1.5 完整管道和重定向系统 (优先级: CRITICAL)

**现状**: 基础框架存在，缺少完整功能
**依赖**: 无（可立即开始）
**阻塞**: P1.3（作业控制）和P1.7（POSIX内置命令）

**待实现子任务**:

1. [ ] 多命令管道执行 (`cmd1 | cmd2 | cmd3`)
2. [ ] 输出重定向 (`>`, `>>`)
3. [ ] 输入重定向 (`<`)
4. [ ] 错误重定向 (`2>`, `2>>`)
5. [ ] 文件描述符操作 (`n>&m`, `n<&m`, `>&-`, `<&-`)
6. [ ] Here文档 (`<< EOF`)
7. [ ] 重定向顺序评估
8. [ ] 单元测试

**技术要点**:

- 使用os_pipe库（已在Cargo.toml）
- 使用nix::fcntl进行fd操作
- POSIX重定向语义严格遵循规范
- 管道中的fd关闭时序关键

**代码位置**:

- Lexer: `src/modules/lexer.rs` (添加重定向token识别)
- Parser: `src/modules/parser.rs` (解析管道和重定向语法)
- SSA IR: `src/modules/ssa_ir.rs` (添加管道/重定向指令)
- Executor: `src/modules/ssa_executor.rs` (执行管道和重定向)

### 第二优先级

- [ ] **P1.6 Shell选项系统** - `set -e/-u/-x/-n/-v/-o` 
- [ ] **P1.3 作业控制系统** - 需依赖P1.5完成
- [ ] **P1.7 完整POSIX内置命令** - 需依赖P1.3和P1.5完成

### 第三优先级 (交互式功能，暂不进行)

- [ ] **交互式反斜杠续行** - `\`换行支持
- [ ] **交互式多行模式** - 括号/引号未闭合时进入多行
- [ ] **交互式信号处理** - Ctrl+C/D支持

## 行动项

1. **分析当前的管道/重定向实现** ✓ 已完成
   
   - 查看src/modules/pipeline.rs现有代码
   - 检查SSA IR中的管道指令定义

2. **设计Parser对重定向语法的识别**
   
   - 添加重定向token (>, >>, <, 2>, 2>>, &>, <<)
   - 修改parse_simple_command()识别重定向
   - 在Parser中构建重定向AST节点

3. **扩展SSA IR**
   
   - 添加Pipe、Redirect、HereDoc指令
   - 定义指令的参数格式

4. **实现Executor执行逻辑**
   
   - 实现管道创建和fd连接
   - 实现重定向文件打开和关闭
   - 处理Here文档的字符串收集

5. **编写验证测试**
   
   - 单个命令重定向测试
   - 多命令管道测试
   - 复杂重定向组合测试

## 技术决策

- **Windows兼容性**: 使用Rust库（os_pipe, nix）而非系统调用
- **SSA IR优先**: 所有语义在IR中明确表示，Executor仅执行
- **POSIX严格遵循**: 参考dash/bash的重定向实现逻辑

## 验收标准

完成P1.5后，应能通过:

- ✅ `echo hello | grep hello` 输出匹配行
- ✅ `echo test > /tmp/test.txt` 创建文件
- ✅ `cat < /tmp/test.txt` 读取文件内容
- ✅ `cmd 2> error.log` 错误重定向
- ✅ `<< EOF` Here文档多行输入
